# HappyFreeTime 学习进度

_学习状态与代码交付状态分开维护 · 最后更新：2026-08-27_

---

## 📏 掌握等级

| 等级 | 判断标准 |
| --- | --- |
| `L0 未开始` | 尚未系统阅读或实验 |
| `L1 能识别` | 看见代码或术语能说明大致用途 |
| `L2 提示下解释` | 在场景或提示下能解释数据流和原因 |
| `L3 独立验证` | 不看答案能解释，并能修改测试或完成小实验 |
| `L4 迁移应用` | 能处理变体、比较替代方案并承受面试深挖 |

## 📍 里程碑总览

| 里程碑 | 代码状态 | 学习状态 | 当前证据 | 下一步 |
| --- | --- | --- | --- | --- |
| M1 核心入口闭环 | 已完成，包括请求幂等、历史切换与刷新恢复 | `L2 提示下解释` | Session View/API 回归、Request ID 重试测试、102 条全量回归和本地真实浏览器主路径 | 闭卷串讲全链路，再完成完整答辩 |
| M2 可信规划 | A/B/C1-C3/S0/D0-D3 已实现首版，D4 部分完成，F 的真实地图单向渲染已完成首版，整体未完成 | `L0 未开始` | 四个显式多站骨架、每角色 8 个候选的有界枚举、实际营业边界、24 Route Leg 公平预算、真实高德路线/天气/地图联调与 102 条回归 | 补餐时锚点、返程/全程距离、集合级多样化、地图时间线双向联动和可重复浏览器 E2E |
| M3 约束修改与重规划 | 未开始 | `L0 未开始` | 已有未决产品想法 | 等正式验收标准 |
| M4 执行安全闭环 | 未开始 | `L0 未开始` | 仅设计基线 | 后续处理 |
| M5 可观测与评测 | 未开始 | `L0 未开始` | M1 有离线 smoke cases | 后续处理 |

## 🧩 M1 切片进度

| 切片 | 状态 | 用户当前能说明 | 提升到下一等级所需证据 |
| --- | --- | --- | --- |
| 约束来源与默认假设 | 已学习，`L2` | 区分 `user_explicit`、`user_inferred`、`default_rule` 和 `Assumption` | 独立补一条来源边界测试并解释结果 |
| Gate 与 interrupt/resume | 已学习，`L2` | 解释严格预算为何反问，以及 checkpoint 如何恢复 | 独立画出一次恢复链路并改一条 Graph 测试 |
| Planning 候选与冲突 | 已学习，`L2` | 能解释结构化输入、显式骨架、硬约束过滤、可解释评分和无解冲突 | 独立修改一条多站 Planning 变体测试，并解释角色池初筛、组合、Route 复核与评分刷新 |
| FastAPI 与 SQLite | 已学习，`L2` | 能区分业务表与 checkpoint，追踪新请求/恢复请求，并解释 SessionView 与请求幂等 | 独立补一条 API 恢复或重复提交测试，并解释事务失败结果 |
| React 状态消费 | 已学习，`L2` | 能区分临时 UI 状态与可恢复会话状态，并解释 URL 定位、历史切换、反问、重试和互斥响应的 UI | 独立补一个非法响应的组件测试或仓库内 E2E |
| 测试与 smoke eval | 已实验，`L3` | 能区分测试证据边界，并已用真实本地浏览器验证创建、刷新、切换和前进/后退 | 将浏览器主路径固化为仓库内可重复 Playwright 测试 |
| M1 完整答辩 | 未开始 | 尚不能独立串讲全链路 | 完成闭卷链路图、2 分钟讲解和变体追问 |

## 🧠 已形成的关键认识

- Router 负责自然语言到 `Interpretation`，不负责补默认值或查询业务事实
- Enrichment 区分“用户没说”和“用户说了但没解析成功”
- `user_inferred` 是带用户证据的推断，不属于默认 `Assumption`
- Gate 用确定性业务规则决定是否阻断，不让模型自由决定每次是否反问
- interrupt 保存 Graph 状态，回答恢复后重新经过 Router 以处理自由文本和意图改变
- 离线模式只替换语义抽取适配器，后续 Graph、服务、API 和持久化仍真实运行
- Planning 先保证硬约束可行，再用偏好评分排序；高分不能抵消硬约束违反
- `violation` 属于单个候选，全部候选因 violation 被淘汰后才形成整体 `conflict`；可行方案可以带 `tradeoff` 或非阻断 `warning`
- 原生 Planning 使用确定性 `PlanningIntent`、显式 2/3/4 站骨架、有界组合与规则评分；LLM 不参与当前规划选择和硬约束裁决，也不再依赖 V1 fixture ID 查分
- 约束 `source` 回答值从哪里来，`strength` 回答能否违反；两者应正交，当前模型已有前者但尚缺后者
- LLM 适合抽取模糊语义、提出带证据的 Planning Intent、评价可行候选的语义匹配并生成解释；硬约束、外部事实和可行性仍由可测试规则与 Verifier 裁决
- 浏览器地图应渲染 Planner 已复核的 `RouteLeg.geometry`，而不是再次请求路线；JS Key 可以公开，配套 `securityJsCode` 必须通过后端同源代理保护
- 业务表回答“用户拥有什么数据”，checkpoint 回答“工作流运行到哪里”；即使共用一个 SQLite 文件，职责也不同
- `Session` 是长期对话边界，`PlanningRun` 是一次逻辑提交的处理生命周期，`message_id` 不能代替 `run_id`
- `request_id` 应由客户端为逻辑提交生成并在重试时复用，服务端以此避免重复消息、模型调用和方案生成
- `Session Status` 属于稳定业务状态，Graph pending node 属于执行位置；前端通过 `SessionView` 消费产品状态，不读取 checkpoint 内部结构
- 前端以 URL `session` 参数定位活跃会话，启动时消费 Session View；最近会话点击、刷新和浏览器前进/后退共用同一恢复路径
- `sessionId` 只是恢复数据的钥匙；后端还必须持久化相应状态并通过 SessionView 返回，不能把 React 内存状态当作业务事实
- 当前 `selectedPlanId` 只表示正在查看；正式 `Selected Plan` 必须由用户确认并写入后端
- `plans` 与 `conflict` 的互斥应由后端契约保证，再由前端判别联合穷尽渲染，不能让 UI 猜测哪个字段可信
- 计划更新期间可以保留上一 Plan Version 并显示运行状态；成功后标记旧版本被替代，失败后明确仍展示上一版本
- 测试通过只证明测试实际经过的边界和写下来的断言，不能自动证明真实数据、真实模型或完整用户旅程
- RouterExtractor 的 Fake 模型测试证明结构化契约、重试和降级，不证明真实自然语言抽取准确率
- 当前 5 条 smoke case 直接运行 Enrichment、Gate 和 Planning，绕过 Router、Graph、HTTP、SQLite 与 React，因此不是端到端测试
- 用户隔离、最近列表、稳定 Session View、重复 Request ID 与跨会话相同方案组合已有 API 测试；刷新恢复另有一次真实本地浏览器回归
- 前端 build 只证明类型检查和生产打包；本轮手工浏览器回归补充了跨前端、API 与 SQLite 的主路径证据，但仍应固化为可重复 Playwright 测试
- GET Session 回归测试应通过公开 HTTP seam 验证消息顺序、完整方案快照和重复读取稳定性，不直接查询数据库，也不把语义不足的 `status=active` 固化为“规划完成”
- `plan_id` 是候选实例身份，`composition_fingerprint` 才是稳定组合；两者混用会让不同会话生成相同组合时发生持久化主键冲突

## 🎯 最近学习目标

当前六个 M1 行为切片已完成提示式学习，接下来：

1. 不看答案画出完整请求、反问恢复和持久化链路
2. 完成 2 分钟 M1 讲解及至少三轮变体追问
3. 根据答辩暴露的薄弱点选择下一个 L3 小实验
