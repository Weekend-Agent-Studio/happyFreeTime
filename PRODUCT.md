# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 主要用户是在周末临近时，需要为自己、伴侣、孩子或朋友快速安排本地半日或一日活动的人。
- 用户可能只有模糊意愿，希望系统减少选择成本；也可能带着明确的时间、地点、距离、饮食、儿童和预算约束来比较并继续修改方案。
- 项目还面向招聘面试官与技术评审，需要用可运行产品和工程证据说明可信规划、状态恢复、局部修改、可控记忆与评测能力。

## Product Purpose

HappyFreeTime 是一个“可信、可修改、会记住一家人的周末管家”。它把自然语言需求转成最多三个可执行、可比较、可追溯的本地出行方案；用户改变主意时只重排必要部分，并能理解每个取舍、外部事实和记忆影响。

成功不只意味着生成一段行程文字，而是用户能快速理解方案差异、选择或修改方案，并在后续确认、模拟下单、取消、恢复和复盘中保持同一条可审计状态链。

## Positioning

- 可信规划：天气、地理编码、路线、营业、价格和可用性保留来源、核验与降级语义，未知实时事实不由 LLM 补写；作品演示中的商业属性使用明确的确定性 Demo World，而非伪造实时商户事实。
- 克制修改：约束、锁定站点、Plan Version 与 PlanDiff 支持局部重规划，修改一处不无理由推翻整段行程。
- 可控记忆：本人、家庭成员和临时同行的记忆有范围、证据、置信度、有效期与撤销路径，并公开哪条记忆影响了方案。
- 技术上采用“LLM 语义理解 + 确定性规划/验证 + 显式状态机”的混合架构，不以多个 Agent 名称或让 LLM 自由排行程作为差异化。

## Operating Context

- 典型入口是一句自然语言请求；系统可能先提出一个最重要的澄清问题，再返回候选方案。
- 核心决策对象是包含有序站点、时间、花费、Route Leg、风险、取舍和证据的 Plan，而不是一段不可编辑的聊天文本。
- 当前会话必须保留并按时间顺序展示完整的用户与管家来回，包括用户请求、管家反问、用户补充、公开阶段状态、方案回复和后续修改；Plan 是一条结构化的富管家回复，不能取代会话记录。
- 用户需要在同一会话中比较候选、查看站点与路线详情、修改约束、保留或替换局部、确认执行，并能从历史会话恢复完整消息流。
- 当前产品由 React/Vite 前端、FastAPI、SQLite、LangGraph 编排、确定性 PlanningService/Verifier 与真实或回放 Provider 组成。

## Capabilities and Constraints

- 首期城市为北京；支持短时、半日和一日规划，最多四个核心停靠点，输出至多三个满足硬约束且取舍不同的方案。
- 活动、餐厅、咖啡或甜品等角色可以组合；Planner 负责候选与组合，Provider 提供外部事实，Verifier 裁决硬约束，Presenter 只解释已验证结果。
- 天气、地理编码和路线可接入高德；POI 展示信息与规划内核分离。默认 Demo World 保留 OSM 名称/坐标锚点，并对参考价格、营业、评分、评论、排队和预约提供确定性模拟值；其决策字段会进入 Catalog/Planner/Verifier，展示字段经 `PoiPresentation` 返回。页面必须一次明确标识数据模式，不逐字段制造噪声。
- 方案展示必须让用户在不展开详情时快速看清主题、总时长、全程距离、预算、主要站点、交通衔接、推荐理由和最大风险；完整证据渐进披露。
- 历史会话列表与会话内消息历史是两个层级：前者用于切换 Session，后者是当前 Session 的完整对话；两者都需要恢复，但历史列表不能挤占核心决策空间。
- 桌面端使用一个按需出现的上下文检查器承载行程/POI 与通勤详情、共享三方案 RouteLeg geometry 的高德地图、执行后的订单，以及来源/依据；它们是同一 Plan 的不同视图，不产生第二套事实。具体 Tab 与默认开闭状态仍可在构图阶段调整。
- 当前 Provider 调用由 Graph、Service 和确定性代码编排，不是 LLM 在 ReAct 循环中自由选择工具。界面可以展示“理解需求、补全地点、核验天气、复核路线、验证营业”等公开阶段、来源与降级状态，但不能把它叙述成模型自主工具调用，也不展示 chain-of-thought。
- 长期记忆、优惠券与回忆属于后续能力，当前产品形态不能依赖这些功能，也不能伪造已实现数据。尚未确认的方向包括：用记忆生成可点击的倾向/场景建议，点击后形成一次显式用户请求并触发规划；以及用独立抽屉查看、纠正和删除记忆。优惠券若进入产品，首版只需独立橱窗或抽屉。以上保持为开放设计，不进入 M2.5 的主信息层级。
- 当前 M2 已完成可信规划与离线验收；M2.5 的 Demo World 和 POI 详情已经完成首版，不能改变 Planner 的可行性语义，也不能产生第二套路线或事实。当前仍无真实库存、支付或订单履约。

## Brand Commitments

- 产品名为 HappyFreeTime，对外角色是自然、克制、可靠的家庭周末管家，而不是旅行搜索引擎、工程控制台或拟人化过度的聊天机器人。
- 用户明确希望整体更暖、更自然；视觉可以进行较大结构调整，不受现有左中右布局约束。
- 用户确认的视觉偏好是现代、简约、轻盈且顺滑：大留白、柔和环境色、少量深色对比、干净圆角和克制阴影。参考图只约束质感，不复制其品牌、内容或页面功能；避免厚重纸张、布料、工业材质和过多实体隐喻。
- 当前视觉方向以“晴空雾光”为基础，配色转为日光黄、奶油暖白与清透淡蓝交织，避免大面积红橙偏色；颜色浓度属于设计系统选择，不作为最终用户的规划设置。磨砂玻璃主要服务于外壳、悬浮检查器和控制层，核心数据卡保持足够不透明度与对比度。
- 方案卡标题本身负责概括策略，不再为每张卡重复一条同义“最强理由”。只有当前选中的方案在行程检查器或移动端展开态中展示较完整的推荐理由与关键取舍。共享天气只出现一次；返程时间仅在用户明确提出最晚到家约束时显示，并保持为普通元数据；方案特有风险使用低饱和提醒，不以红蓝高亮文字抢占注意力。
- 中文界面保留实用的系统字体回退；风格服务于方案扫描、比较、可信状态与重复使用，不以装饰性动效为主。

## Evidence on Hand

- 已确认产品与技术基线：`docs/canonical/architecture_v2.md`、`docs/canonical/v2_roadmap.md`。
- 当前视觉基线：`design-system/happyfreetime/MASTER.md`；“日光晴蓝”已获确认并落入生产前端与响应式规范。
- 可运行前端：`frontend/src/`；已有桌面与 375px 离线 Playwright 路径。
- M2 包含真实/回放天气、地理编码、路线与可用性事实、统一 Repair Contract、Presenter、19 条声明式 smoke 与 HTTP/SQLite E2E。
- 当前没有可对外宣称的真实库存、真实支付、真实用户记忆、优惠券资产或用户回忆内容；未来设计不能把这些预留包装成已上线功能。

## Product Principles

1. 首屏先帮助用户做选择，再让用户检查证据；事实可查但不抢占决策注意力。
2. 三个候选使用一致的信息结构，差异来自内容、策略与风险，而不是展示权重不公平。
3. 方案、时间线、地图、Route Leg 和 POI 详情引用同一结构化对象，不复制或改写规划事实。
4. 模板 Presenter 与确定性主交互必须独立于 LLM 可用；LLM 只能做有界理解、启发或解释。
5. 历史、记忆和执行能力围绕当前计划形成连续工作流，不把产品退化为消息列表或功能入口集合。

## Accessibility & Inclusion

- 交互控件可键盘访问，具有清晰焦点；触控目标至少 44px。
- 关键状态不只依赖颜色；桌面与 375px 移动端都应保持可完成的主路径。
- 支持 `prefers-reduced-motion`，动效只用于选中、展开、定位与阶段状态等可解释变化。
